server:
  port: 8082

spring:
  datasource:
    url: ${USER_DB_URL:jdbc:postgresql://localhost:5432/usersdb}
    username: ${USER_DB_USER:postgres}
    password: ${USER_DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: health,info

account-service:
  url: ${ACCOUNT_SERVICE_URL:http://localhost:8080/api}
